# TODO

- [ ] Remove `<video class="hero__video">` from `src/components/Hero.astro`.
- [ ] Replace it with a non-video placeholder container that can render ASCII art (as user will add).
- [ ] Ensure layout/CSS still works (no breaking references to `.hero__video`).
- [ ] Run Astro build (or lint) to verify no errors.

