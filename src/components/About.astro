<section class="about" aria-labelledby="about-title" id="about">
	<div class="about__inner">
		<h2 id="about-title">About Me</h2>

		<p class="about-intro">[Short personal introduction missing]</p>

		<div class="about__cards">
			<article class="about-card">
				<h3>Why engineering?</h3>
				<ul>
					<li>Two types of joy: creation (lasts) vs consumption (fleeting)</li>
					<li>Engineering gives me creation joy — building things that persist</li>
				</ul>
			</article>

			<article class="about-card">
				<h3>Why robotics?</h3>
				<ul>
					<li>Most multidisciplinary field — mechanical, electrical, CS converge here</li>
					<li>Lets me create nearly anything I can imagine</li>
				</ul>
			</article>

			<article class="about-card">
				<h3>Current academic focus</h3>
				<ul>
					<li>At my current institution, I am pursuing ECE as my major and Robotics as minor</li>
					<li>Personal study: <em>Design of Machinery</em> (Norton), <em>Intro to Robotics</em> (Niku), <em>Modern Robotics</em> (Lynch & Park)</li>
				</ul>
			</article>

			<article class="about-card">
				<h3>Goals</h3>
				<div class="goals__subsections">
					<div class="goals__subsection">
						<h4>Long-term</h4>
						<ul>
							<li>PhD in bio-inspired robotics</li>
							<li>Research scientist at bionic learning network labs (e.g., Festo)</li>
						</ul>
					</div>
					<div class="goals__subsection">
						<h4>Short-term</h4>
						<ul>
							<li>[short-term goals missing, add about 50 words here]</li>
						</ul>
					</div>
				</div>
			</article>

			<article class="about-card">
				<h3>What I'll contribute</h3>
				<ul>
					<li>Undergraduate robotics research & open-source software</li>
					<li>Student mentorship, leadership roles, robotics workshops</li>
					<li>Cross-disciplinary collaborative engineering projects</li>
				</ul>
			</article>

		</div>
	</div>
</section>

<style>
	.about {
		padding: clamp(3rem, 8vw, 6rem) clamp(1.5rem, 7.5vw, 6rem);
		background: var(--bg-white);
	}

	.about__inner {
		max-width: 100%;
		margin: 0 auto;
		display: flex;
		flex-direction: column;
		gap: clamp(1.5rem, 4vw, 3rem);
	}

	.about h2,
	.about h3,
	.about p {
		margin: 0;
	}

	.about__inner > * {
		max-width: 100%;
	}

	.about__cards {
		display: grid;
		grid-template-columns: 1fr;
		gap: clamp(1.5rem, 3vw, 2.25rem);
		width: 100%;
	}

	.about-card {
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
		padding: clamp(2.25rem, 4.5vw, 3rem);
		background: var(--bg-white);
		border-radius: 18px;
		border: 1px solid var(--card-border);
		box-shadow:
			0 3px 12px rgba(0, 0, 0, 0.045),
			inset 0 1.5px 0 rgba(255, 255, 255, 0.75);
		transition: box-shadow 0.2s ease, transform 0.2s ease;
	}

	.about-card:hover {
		box-shadow:
			0 12px 36px rgba(0, 0, 0, 0.09),
			inset 0 1.5px 0 rgba(255, 255, 255, 0.75);
		transform: translateY(-3px);
	}

	.about h2 {
		font-size: clamp(1.75rem, 3.75vw, 3rem);
		line-height: 1.1;
		text-align: center;
	}

	.about h3 {
		font-size: clamp(1.25rem, 1.8vw, 1.75rem);
		line-height: 1.3;
		color: var(--text-primary);
	}

	.about p {
		font-size: clamp(1rem, 1.2vw, 1.125rem);
		line-height: 1.7;
		color: var(--text-primary);
		opacity: 0.9;
	}

	.about ul {
		margin: 0;
		padding-left: 1.5rem;
		display: grid;
		gap: 0.5rem;
	}

	.about li {
		font-size: clamp(0.9375rem, 1.1vw, 1.0625rem);
		line-height: 1.6;
		color: var(--text-primary);
		opacity: 0.85;
	}

	.goals__subsections {
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
	}

	.goals__subsection h4 {
		font-size: clamp(0.9375rem, 1.2vw, 1.125rem);
		line-height: 1.4;
		color: var(--accent);
		font-weight: 600;
		margin: 0 0 0.5rem 0;
	}

	.goals__subsection ul {
		padding-left: 1.5rem;
		gap: 0.5rem;
	}

	.about-intro {
		font-size: clamp(1.425rem, 1.5vw, 1.575rem);
		line-height: 1.7;
		color: var(--text-primary);
		opacity: 0.9;
		text-align: center;
		max-width: 800px;
		margin: 0 auto;
	}

	@media (min-width: 768px) {
		.about__cards {
			grid-template-columns: repeat(auto-fit, minmax(min(100%, 34ch), 1fr));
		}
	}
</style>
