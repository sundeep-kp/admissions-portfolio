<section class="contact section-fade" aria-labelledby="contact-title" id="contact" style="background: var(--contact-bg); color: var(--text-on-dark);">
	<div class="contact__inner">
		<h2 id="contact-title">Contact</h2>

		<div class="contact__links" aria-label="Contact links">
			<a href="https://canva.link/0zsjufwpj3qsscc" target="_blank" rel="noopener noreferrer">Download Resume</a>
			<a href="https://github.com/sundeep-kp/ros2-roadmap-ultralab/">GitHub</a>
			<a href="https://www.linkedin.com/in/sundeepskp">LinkedIn</a>
			<a href="mailto:panigrahisundeep99@gmail.com">Email</a>
			<a href="mailto:halfwiredtv@email...">Half Wired TV</a>
		</div>
	</div>
</section>

<style>
	.contact {
		padding: clamp(3rem, 8vw, 6rem) var(--section-padding-inline);
	}

	.contact__inner {
		width: 100%;
		margin: 0 auto;
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: clamp(2.25rem, 4.5vw, 4.5rem);
		text-align: center;
	}

	.contact__links {
		display: flex;
		flex-wrap: wrap;
		justify-content: center
		gap: 1.5rem;
	}

	.contact h2 {
		margin: 0;
		font-size: clamp(1.75rem, 2.5vw, 2.5rem);
		line-height: 1.1;
	}

.contact a {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		padding: 1.125rem 1.5rem;
		text-decoration: none;
		color: var(--text-on-dark);
		background: var(--contact-link-bg);
		border-radius: 12px;
		border: 1px solid var(--contact-link-border);
		transition: background 0.2s;
		font-size: clamp(1rem, 1.2vw, 1.125rem);
		font-weight: 500;
	}

	.contact a:hover {
		background: var(--contact-link-hover);
		transform: translateY(-1.5px);
	}
</style>