<section class="hero" aria-labelledby="hero-title">
	<div class="hero__inner">
		<div class="hero__content">
			<p>Name</p>
			<h1 id="hero-title">[Name Missing]</h1>

			<p>[Introduction Missing]</p>

			<p>[University Missing]</p>

			<p>Research Interests</p>
			<ul>
				<li>[Research interest missing]</li>
				<li>[Research interest missing]</li>
				<li>[Research interest missing]</li>
			</ul>

			<div class="hero__buttons" aria-label="Buttons">
				<a href="[resume link missing]">Resume</a>
				<a href="[github link missing]">GitHub</a>
				<a href="[linkedin link missing]">LinkedIn</a>
				<a href="mailto:[email missing]">Email</a>
			</div>
		</div>

		<div class="hero__visual" aria-label="Ornithopter video placeholder">
			<div class="hero__video-placeholder">[Ornithopter video placeholder]</div>
		</div>
	</div>
</section>

<style>
	.hero {
		min-height: 100vh;
		padding-inline: clamp(1rem, 5vw, 4rem);
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.hero__inner {
		width: 100%;
		max-width: 1200px;
		display: flex;
		flex-direction: column;
		gap: clamp(1.5rem, 3vw, 5rem);
	}

	.hero__content,
	.hero__visual,
	.hero__video-placeholder {
		min-width: 0;
	}

	.hero__content {
		display: flex;
		flex-direction: column;
		gap: clamp(0.75rem, 1.5vw, 1.25rem);
	}

	.hero__buttons {
		display: flex;
		flex-wrap: wrap;
		gap: clamp(0.75rem, 1.5vw, 1.25rem);
	}

	.hero__visual {
		width: 100%;
	}

	.hero__video-placeholder {
		aspect-ratio: 16 / 9;
		width: 100%;
		display: flex;
		align-items: center;
		justify-content: center;
		padding: clamp(1rem, 2vw, 1.5rem);
	}

	.hero p,
	.hero h1,
	.hero ul {
		margin: 0;
	}

	.hero h1 {
		font-size: clamp(2.25rem, 5vw, 5rem);
		line-height: 0.95;
	}

	.hero p,
	.hero li,
	.hero a {
		font-size: clamp(1rem, 1.1vw, 1.125rem);
		line-height: 1.6;
	}

	.hero ul {
		padding-left: 1.25rem;
	}

	@media (min-width: 768px) {
		.hero__inner {
			flex-direction: row;
			gap: clamp(2.5rem, 5vw, 5rem);
		}

		.hero__content {
			flex: 0 0 55%;
			max-width: 55%;
		}

		.hero__visual {
			flex: 0 0 45%;
			max-width: 45%;
		}

		.hero__buttons {
			justify-content: flex-start;
		}
	}
</style>
