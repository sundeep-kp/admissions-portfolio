<section class="learning section-fade" aria-labelledby="learning-title" id="learning" style="background: var(--bg-white); color: var(--text-primary);">
	<div class="learning__inner">
		<h2 id="learning-title">Currently Learning</h2>

		<div class="learning__grid">
			<article class="card card--light learning-card">
				<h3>Book</h3>
				<ul>
					<li>design of machinery by rl norton</li>
					<li>intro to robotics by saeed b niku</li>
					<li>modern robotics by lynch and park</li>
				</ul>
			</article>

			<article class="card card--light learning-card">
				<h3>Current Project</h3>
				<ul>
					<li>RL based pick n place robotic arm from scratch</li>
				</ul>
			</article>

			<article class="card card--light learning-card">
				<h3>Research Paper</h3>
				<ul>
					<li>Gait Optimisation of legged robotics</li>
				</ul>
			</article>

			<article class="card card--light learning-card">
				<h3>Next Goal</h3>
				<ul>
					<li>[description missing, add about 50 words here]</li>
				</ul>
			</article>
		</div>
	</div>
</section>

<style>
	.learning {
		padding: clamp(3rem, 6vw, 6rem) clamp(1rem, 5vw, 4rem);
	}

	.learning__inner {
		max-width: 1200px;
		margin: 0 auto;
		display: flex;
		flex-direction: column;
		gap: clamp(1.5rem, 3vw, 3rem);
	}

	.learning__grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(min(100%, 34ch), 1fr));
		gap: clamp(1rem, 2vw, 1.5rem);
		width: 100%;
	}

	.learning-card {
		display: flex;
		flex-direction: column;
		gap: 1rem;
		min-width: 0;
		width: 100%;
		padding: clamp(1.5rem, 3vw, 2rem);
		border-radius: 12px;
		border: 1px solid var(--card-border);
		box-shadow:
			0 2px 8px rgba(0, 0, 0, 0.03),
			inset 0 1px 0 rgba(255, 255, 255, 0.5);
		transition: box-shadow 0.2s ease, transform 0.2s ease;
	}

	.learning-card:hover {
		box-shadow:
			0 8px 24px rgba(0, 0, 0, 0.06),
			inset 0 1px 0 rgba(255, 255, 255, 0.5);
		transform: translateY(-2px);
	}

	.learning h2,
	.learning h3,
	.learning ul,
	.learning li {
		margin: 0;
	}

	.learning h2 {
		font-size: clamp(1.75rem, 2.5vw, 3rem);
		line-height: 1.1;
	}

	.learning h3 {
		font-size: clamp(1.125rem, 1.4vw, 1.5rem);
		line-height: 1.25;
		color: var(--text-primary);
	}

	.learning ul {
		padding-left: 0;
		list-style: none;
		display: grid;
		gap: 0.5rem;
	}

	.learning li {
		font-size: clamp(0.95rem, 1vw, 1.05rem);
		line-height: 1.7;
		color: var(--text-primary);
		opacity: 0.9;
	}

	/* auto-fit handles desktop expansion without fixed breakpoints */
</style>